<nav class="navbar navbar-default menu" >
  <div class="container-fluid">
    <div class="navbar-header">
      <%= image_tag("iwmp_logo.jpg", :alt => "I Wet My Plants Cartoon", :height => "150", :style => "border-radius: 50%;") %>
      <h1 class="logo_words">I Wet My Plants</h1>
    </div>
    <ul class="nav navbar-nav">
      <i class="fas fa-bars"></i>
      <li><%= link_to "Home", plants_path %></li>
      <li><%= link_to 'Water All Plants', water_all_plants_path, method: :patch %></li>
      <div class='tweet-option'>
        <% if current_user.garden.tweet %>
          <li><%= link_to 'Turn Tweets Off', stop_tweets_path, method: :patch %></li>
        <% else %>
          <li><%= link_to 'Turn Tweets On', start_tweets_path, method: :patch %></li>
        <% end %>
      </div>
      <li><%= link_to 'Logout', logout_path, method: :delete %></li>
    </ul>
  </div>
</nav>

<% if flash.any? %>
  <div class="flash">
    <% flash.each do |key, value| %>
      <%= value %>
    <% end %>
  </div>
<% end %>
