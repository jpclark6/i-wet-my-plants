<%= render "layouts/nav" %>
<div class='dashboard-body'>
  <div class='main-heading'>
    <h1 class='garden-header'>My Gardens</h1>
  </div>
  <% @gardens.each do |garden| %>
    <div class='garden'>
      <h2><%= garden.name %></h2>
      <%= link_to "Add Plant", add_plant_path(garden), class: 'btn btn-outline-primary btn-md' %>
      <div class="container">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Name</th>
              <th>Species</th>
              <th>Hours Until Watering</th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% garden.plants.each do |plant| %>
              <tr class='plant' id='plant-<%= plant.id %>'>
                <td><%= plant.name %></td>
                <td><%= plant.species %></td>
                <td><%= plant.hours_until_watering %></td>
                <td><%= link_to "Edit", edit_plant_path(plant) %></td>
                <td><%= link_to "Water Plant", water_plant_path(plant), method: :post %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>
</div>